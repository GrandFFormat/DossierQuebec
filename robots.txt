User-agent: *
Allow: /

# Les volets municipaux (/quebec/, /montreal/) sont volontairement hors des moteurs de recherche
# en attendant la réponse de la Ville de Québec. Il n'est PAS bloqué ici, et c'est
# délibéré : un robot à qui l'on interdit de lire la page ne voit jamais la balise
# « noindex » qu'elle contient, et peut alors lister l'adresse toute nue. On le laisse
# donc lire, et on lui dit de ne pas indexer — par l'en-tête X-Robots-Tag (vercel.json)
# et par la balise meta robots de chaque page.

Sitemap: https://dossierquebec.ca/sitemap.xml
